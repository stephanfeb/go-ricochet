# Backlog

The open findings from the [2026-09-13 release audit](audit-2026-09-13/README.md)
that sat below its blocker line, plus anything found since. The eleven
blockers are done (see the status note at the top of the audit). This file
is the working list: one item at a time, one commit per item, the full
suite on a fresh database after each, the row updated in the same commit.

**How to use this file.** Pick the next `open` item, verify its anchors
(line numbers drift), fix it, add the test that would have caught it, mark
the row `done <date>` naming the test, in the same commit as the fix
(`git log -S<test name>` finds it). When an item is deliberately not going
to be fixed, mark it `wontfix` with one line of reasoning so it is not
re-investigated. Anchors are `file:line` as of the commit that last edited
this file.

Suggested order: the four correctness bugs a user can hit today (P5, P4,
P3, S1), then the one security item with no workaround (S2), then the rest
by section.

## Security

| ID | Status | Item |
|----|--------|------|
| S1 | done 2026-09-13 | **Patch If-Match was checked outside the transaction.** `internal/storage/postgres/postgres.go:685` `PatchDocument` reads with `GetDocument`, compares `ContentHash` to `ifMatch`, then calls `PutDocument(..., ifMatch=nil)`, so two concurrent patches both pass the check and the second overwrites the first. Fixed: `PatchDocument` now begins its own transaction, reads the row with `SELECT ... FOR UPDATE`, checks If-Match, merges, and writes through `putDocumentLocked` (the body of `PutDocument`, now shared) before committing. This also closes the lost update between two patches without If-Match. Tests: `TestConcurrentPatchesWithSameIfMatchAdmitOne`, `TestConcurrentPatchesWithoutIfMatchAllApply` (`internal/storage/postgres`; the second fails reliably on the old code, the first only when the race lands). Audit A M-1, B H6. |
| S2 | done 2026-09-13 | **Registry and presence trusted the JSON `server_id`, not the GossipSub signer.** `internal/registry/registry.go:218-236` uses `msg.ReceivedFrom` only to skip its own messages, then stores the announcement under `info.ServerID` from the payload. Presence events (`internal/presence/events.go:34,59,73`) are decoded the same way. Any peer on the topic can overwrite another server's entry or publish presence for peers it does not have. Fixed: `Registry.handleAnnouncement(from, data)` keys by `msg.GetFrom()` (the StrictSign-verified publisher) and refuses a payload naming another server; `Tracker.handleMessage(from, serverID, data)` refuses a message on a server's topic unless the signer is that server and the payload names it. Tests: `TestAnnouncementMustBeSignedByItsServer`, `TestForgedAnnouncementOverGossipSubIsIgnored` (three forge nodes over real GossipSub; fails on the old code), `TestTrackerIgnoresPresenceFromAnotherPublisher`. The Dart client's own registry/presence consumers (`../ricochet-dart-client`) still trust the payload: noted as N4. Audit A M-3. |
| S3 | done 2026-09-13 | **Raw Postgres error text reached clients.** `internal/protocol/mma/handler.go:234,266,292,327,390,443` and `internal/protocol/msa/handler.go:182,280` put `err.Error()` in the response; SCA does the same. A constraint name or SQL fragment is an information leak and a wire contract nobody meant to make. Fixed: `wire.ClientMessage(err)` returns the wording of a recognised client-facing error (mailbox full, quota, forwarding refused, validation, not-found sentinels, conflicts, JSON decode, rate limit, overload) and a fixed phrase per status for anything else; `wire.LogRejection(sc, status, err)` logs the real error with peer, protocol and stream id (there is no request id). Applied in every protocol's pipeline error writer, both MSA submit paths, SCA query, and the thirteen MMA "failed to …: %v" responses via `failed()`. Also classifies `mta.RateLimitError` as 429 and `mta.ValidationError` as 400 (both were 500). Tests: `TestClientMessageHidesDatabaseErrors`, `TestClientMessageKeepsClientFacingErrors` (`internal/protocol/wire`), `TestStorageErrorsAreNotEchoedToClients` (integration; closes the pool under a live server, fails on the old code). Audit A M-6. |
| S4 | done 2026-09-13 | **Directory search was an unbounded, unescaped `ILIKE`.** `internal/storage/postgres/postgres.go:919` builds `coalesce(display_name,'') ILIKE $n OR coalesce(bio,'') ILIKE $n` with `%q%`; `%` and `_` in the query are wildcards, there is no length cap, and the FTS index is unused. Fixed: the query is capped at `storage.MaxDirectoryQueryLength` (100 bytes, `ErrDirectoryQueryTooLong` → 400), reduced to its letters and digits, and rendered as a prefix tsquery (`'ali':* & 'cryp':*`) against the `idx_directory_fts` expression, so the search is index-backed (Bitmap Index Scan verified) and carries no pattern or tsquery syntax; a query with no words returns an empty page without touching the database. No `ILIKE` remains. The storage Info log of the full SQL and args, and the handler's Info log of the query text, are now Debug and carry only booleans. Test: `TestBrowseDirectoryTreatsWildcardsLiterally` (`internal/storage/postgres`; fails on the old code). Audit A M-6, B M3. |
| S5 | done 2026-09-13 | **Every per-peer limit is keyed by a free identity.** Rate limits, admission per-peer caps, mailbox quotas and forwarding trust are all per peer ID, which costs nothing to mint. Decided: per-peer limits are fairness controls; the abuse controls are `max_concurrent_connections` and admission control, which no identity rotation escapes. Found on the way: go-libp2p's resource manager was silently applying its own per-address policy to every host, 8 concurrent connections and 0.2 new connections/s per IPv4 address (IPv6 /56), which turns away clients sharing a carrier-NAT or office address. go-p2p-forge v0.1.2 now builds the resource manager explicitly in all cases, drops the per-address rate limit, and exposes `max_connections_per_ip` (`host.Config.MaxConnectionsPerIP`), wired from ricochet's `performance.max_connections_per_ip`, default 0 = no cap. README Security has a new "Identity Is Free" subsection saying all of this. Tests: `TestResourceManagerPerAddressPolicy` (forge `host`), `TestPerIPConnectionCapConfig` (`internal/core`). Audit A M-7. |
| S6 | done 2026-09-13 | **Unbounded client strings were persisted as index keys.** Folder path was bounded in B6 (`core.MaxFolderPathLength`). Still unbounded: document content type (`internal/protocol/sda/handler.go`), collection key and name (`sca`), feed title/description (`sfa`), directory display name and bio (`sda` directory ops). `validatePath` exists in each handler (`sfa:677`, `sda:1072`, `sca:715`) and is the place to add sibling validators. Fixed: bounds are public constants in `pkg/wire/limits.go` (content type, collection name and key, display name, avatar hash 256 B; feed title 256 B; description and bio 4 KiB; extras 4 KiB as JSON; entry type 64 B); `wire.CheckString`/`CheckJSONSize` (`internal/protocol/wire/fields.go`) return a `FieldError` classified 400 with the field named. Applied in SDA put and batch put (content type), directory join and the directory-listing document materialisation (which now logs and skips an over-bound listing), the SCA validation middleware (key, name) and the SFA validation middleware (title, description, entryType). Tests: `TestCheckString`, `TestCheckJSONSize` (`internal/protocol/wire`), `TestClientStringsAreBounded` (integration, one refusal per field plus values at the bound; fails on the old handlers). Audit A M-8. |
| S7 | done 2026-09-13 | **World-readable stores were undocumented.** Documents, feeds, collections and the directory are readable by any peer by design; README never said so. Done: README Security opens with "What Any Peer Can Read", a per-store table of who reads and who writes (verified against the owner checks in `sda`/`sfa`/`sca` handlers: reads open, writes owner-only, collaborative feeds take `APPEND` from anyone), plus the fact that the server never encrypts store contents; the feature list links to it; `doc/MAILBOX_LIFECYCLE_AND_ACLS.md` gained Gotcha 6 saying ACLs are a mailbox thing. No code; no test. Audit A I-1. |
| S8 | done 2026-09-13 | **Encryption had no forward secrecy and was not bound to the envelope.** `pkg/client/encryption.go` NaCl box with static X25519 keys derived from the identity; ciphertext is not bound to sender/recipient/folder, so a captured ciphertext can be replayed into another folder. Done (the minimum): `EncryptBoundPayload`/`DecryptBoundPayload` in `pkg/client/encryption.go` seal a header of recipient peer ID, folder path (empty = `inbox`, matching delivery) and message ID inside the box, format `"RCE2" nonce box(header‖payload)`; retrieve checks it against the envelope and refuses a relabelled ciphertext; the legacy unbound format still opens (reported as unbound). The Dart client never implemented this layer (OverNode runs Signal sessions above it), so no lockstep. README E2E section now states the model: payload confidentiality/integrity, placement binding, no same-folder replay detection, no forward secrecy by design (ratchet above, as OverNode does), deniable, envelope and `encrypted` flag unauthenticated, one identity key for Noise/GossipSub/DH. Tests: `TestBoundPayloadRefusesRelabelling`, `TestLegacyPayloadStillOpens`, `TestBindingForNormalisesFolder` (`pkg/client`), `TestEncryptedMessageBoundToItsFolderRoundTrips` (integration). Forward secrecy itself: wontfix at this layer, by design as documented. Audit A L-3. |

## Performance

| ID | Status | Item |
|----|--------|------|
| P1 | done 2026-09-13 | **Submission is five round-trips plus a count plus a trigger.** Now three: `StoreMessage` runs one `UPDATE mailboxes SET current_sequence+1, message_count+1, last_access_at=NOW() WHERE id=$1 AND (max_messages<=0 OR message_count<max_messages) RETURNING current_sequence` under the row lock, so the cap is enforced where the sequence is claimed (the B7 quota TOCTOU is closed for messages); `trg_message_access` and its function are dropped; a statement-level `AFTER DELETE ... REFERENCING OLD TABLE` trigger keeps `message_count` in step with every delete path; `ReconcileMessageCounts` runs in maintenance as the safety net; per-store public pruning is gone, a full mailbox with a `retention_count` is pruned on the full path and the store retried once. `storage.MailboxFullError` is the type now (`mailboxes.MailboxFullError` aliases it). Before/after: 6,020–6,070 → 6,750–7,070 req/s, p50 1.6 → 1.3ms (`doc/BASELINES.md`). Tests `TestConcurrentStoresNeverExceedCap`, `TestMessageCountFollowsDeletes`, `TestStoreMessageAdvancesLastAccess`, `TestZeroCapMeansUncapped`, `TestReconcileMessageCountsRepairsDrift` (`internal/storage/postgres/message_count_test.go`), `TestParallelDeliveriesNeverExceedCap` (admits 19 into a cap of 5 on the old code), `TestRollingWindowMailboxNeverFills` (`test/integration/mailbox_cap_race_test.go`). Was: `internal/storage/postgres/postgres.go:236` `StoreMessage`: tx begin, sequence `UPDATE ... RETURNING`, insert, then `trg_message_access` (`schema.sql:287-297`) updates the mailbox row again inside the same tx; the MDA adds a `COUNT(*)` for the cap and, for public mailboxes, two DELETEs with a sort per message. Fix: fold the count into the sequence update (`UPDATE mailboxes SET seq = seq+1, message_count = ...`), drop the trigger in favour of the same statement, move public-mailbox pruning to maintenance. Measure with `ricochet-bench` before and after (`doc/BASELINES.md`). Audit B H1. |
| P2 | done 2026-09-13 | **Three or four Info log lines per request on the hot path.** Now one: `wire.AccessLog(protocol, fallbackOp)` (`internal/protocol/wire/accesslog.go`) sits under the metrics middleware in all seven pipelines and writes `msg=request protocol= op= status= peer= duration= stream=` once the response is out; status comes from the response's `StatusCode()` (added to `StoreAck`, `BatchSubmitResponse` with a new 207 `StatusMultiStatus`, `AdminResponse`), or from `Classify` when the request never produced one. Every per-request Info in the handlers, MDA, MTA and storage creates is Debug; maintenance and lifecycle lines stay Info. `LogRejection` now carries the peer too. Tests `TestAccessLogWritesOneInfoLine`, `TestStatusOfPrefersTheResponse` (`wire/accesslog_test.go`), `TestOneInfoLinePerRequest` (`test/integration/access_log_test.go`, counts Info lines through the full server for submit, retrieve and PUT). Was: Counts today: msa 2, maa 4, mma 5, sda 3 (`grep -c Logger.Info internal/protocol/*/handler.go`). Fix: one Info line per request at the response writer (status, op, duration, peer), everything else Debug. Audit B H2. |
| P3 | done 2026-09-13 | **Notifier was built before the presence monitor, and never left a topic.** `internal/server/server.go:442` constructs `mda.NewNotifier(..., s.presenceMonitor, ...)` while `s.presenceMonitor` is assigned at `:455`, so the notifier's presence check (`internal/mda/notifier.go:110`) is always nil and it dials every recipient, online or not. `notifier.go:154` joins a GossipSub topic per shared/public mailbox and nothing leaves. B7 bounded the goroutines (`WithWorkers`). Fixed: the monitor is built first and passed as an `mda.PresenceChecker` (guarded so a nil monitor never becomes a non-nil interface; startup logs `presence_checked`); the notifier tracks joined topics, leaves one on `DeleteMailbox` and leaves any idle for 15 min on a sweep started with the server. Needed `Node.LeaveTopic` in go-p2p-forge v0.1.1. Tests: `TestNotifierSkipsOfflineRecipient`, `TestNotifierLeavesTopicOnMailboxDelete`, `TestNotifierLeavesIdleTopics` (`internal/mda`); `TestLeaveTopicUndoesJoin` (forge `node`). Audit B H3, C M2. |
| P4 | done 2026-09-13 | **Presence marked every still-connected peer offline after 120 s.** `internal/presence/service.go:103` writes `connectedPeers[pid] = time.Now()` at connect only; the sweep at `:331-336` evicts anything older than `TimeoutDuration`. A peer connected for two minutes is announced offline while its connection is up. Heartbeat (`:291-294`) serialises every online peer ID into one message and hits the 1 MiB pubsub cap around 18k peers. Fixed: the sweep now reconciles the online set against `host.Network().Peers()` on `presence_check_interval` (a peer is online for as long as it is connected; `presence_timeout_sec` is only the TTL advertised on events), and the heartbeat is paged at 4096 IDs with the tracker reassembling a sequence before applying it. Tests: `TestLongConnectedPeerStaysOnline`, `TestReconcileRepairsMissedNotifications`, `TestHeartbeatPagesBoundMessageSize`, `TestTrackerReassemblesPagedHeartbeat` (`internal/presence`). The Dart client library (`../ricochet-dart-client`, `lib/client/presence_tracker.dart`) consumes these heartbeats and gained the same page reassembly (`HeartbeatAssembler`) the same day; ship both sides together. Audit B H4. |
| P5 | done 2026-09-13 | **`GetOrCreateMailbox` was check-then-insert.** Now one `INSERT ... ON CONFLICT ON CONSTRAINT uq_mailbox_owner_folder DO UPDATE SET last_access_at = mailboxes.last_access_at RETURNING ..., (xmax = 0) AS created` in `internal/storage/postgres/postgres.go`; concurrent first deliveries all get the same row, and a later call cannot change an existing mailbox's settings. Tests `TestConcurrentFirstDeliveriesShareOneMailbox`, `TestGetOrCreateKeepsExistingSettings` (`mailbox_race_test.go`); the race test fails on the old code with duplicate-key errors. Audit B H6. |
| P6 | done 2026-09-13 | **Shutdown drains nothing.** Now: every pipeline carries `admission.DrainGate` (before the admission middleware) reading an `admission.Drain` switch the server provides in the registry, and is built `WithActiveStreams(&s.inFlight)`; `Server.Stop` withdraws from `/readyz`, waits `drain_delay`, flips the switch (new requests get a 503 `server shutting down` in each protocol's own error shape; `ErrDraining` classifies as 503 and counts as `overloaded`), waits for in-flight streams up to `ops.shutdown_timeout`, and only then cancels the context, stops services and the host, and closes storage. Handlers are deliberately not removed: a removed handler fails a stream at negotiation with nothing a client can read. Test `TestStopDrainsInFlightRequests` (`internal/server/shutdown_test.go`, real server from config, eight senders in flight through Stop: every outcome is success or 503, at least one of each, drain logged, no pgx closed-pool errors; on the old code no request is refused and nothing drains). Was: Handlers are registered with `host.SetStreamHandler` after `forge.Start` (`internal/server/server.go:489-499`), which forge's `Stop` does not track (`../go-p2p-forge/forge.go:32,134`), and `Server.Stop` (`server.go:161-182`) cancels the context and closes storage under in-flight queries. Fix: on stop, remove stream handlers, wait for in-flight requests (the admission controller knows the count) up to `ops.drain_delay`/`shutdown_timeout`, then close storage. Test: a request in flight during `Stop` completes or gets a clean 503, never a pool-closed error. Audit B M8, C M1. |
| P7 | done 2026-09-13 | **Heavy reads.** (1) `HeadDocument` (new storage method) and `GetDocumentHistory` measure `octet_length(content)` in SQL and never load bodies; `ContentLength` on `DocumentRecord`/`DocumentVersionRecord`; HEAD and HISTORY use them. (2) Collections LIST and QUERY page by keyset: request `cursor`, response `Next-Cursor`; the count is `COUNT(*) OVER()` in the same pass as the page, on a first page only (`X-Total-Count` absent, -1 in the client, on cursor pages); sort is `COALESCE(content->>'f','')`, key as tiebreak; offset still honoured; client `WithQueryCursor`, `ListCollectionKeysPage`, `NextCursor`. (3) `mailboxes.message_bytes` maintained like `message_count` (P1): raised in the `StoreMessage` UPDATE, lowered by the delete trigger, reconciled in maintenance; `ServerStats`, `ListMailboxUsage`, `ListOwnerUsage` read the counters and never touch `stored_messages` (the usage listing looks up `last_message_at` for its page only). On 2,000 mailboxes / 300k messages: stats 61.6 ms → 0.69 ms, owner usage 65.4 ms → 0.45 ms. Retention `OFFSET`s (`feeds.go`, prune paths) are not paging and stay. Tests `TestHeadDocumentReportsSizeWithoutBody`, `TestUsageFiguresComeFromCounters`, `TestQueryCollectionPagesByCursor`, `TestListCollectionKeysPagesByCursor` (`internal/storage/postgres/heavy_reads_test.go`), `TestMessageCountFollowsDeletes` now checks bytes too, `TestCollectionQueryPagesByCursor` (`test/integration/collection_paging_test.go`). Was: `GetDocumentHistory` (`postgres.go:777`) and HEAD load full bodies to report sizes; `QueryCollection` (`collections.go:263`) runs the filter twice (count + page) with `OFFSET` paging (`:241,311`; also `feeds.go:364`); the capacity sampler and operator stats run `SUM(octet_length(payload))` over the whole table (`operator.go:46,105`, `stats.go:62`) every five minutes. Fix: `octet_length` in SQL for sizes, keyset paging, a maintained byte counter or `pg_total_relation_size` for capacity. Audit B M1, M2, M4. |
| P8 | done 2026-09-13 | **Mailbox cache is unbounded and stale.** `internal/mda/delivery.go` `mailboxCache` was a plain map keyed by full path: one entry per folder ever delivered to, cleared only at shutdown, and MMA `updateConfig` wrote `Storage.UpdateMailbox` directly so the cached record kept the old `retention_days`/`retention_count` for the life of the process (after P1 the cap is enforced in the DB, so only the expiry clamp and the rolling-window prune were reading stale settings). Fix: the cache is a `golang-lru/v2` LRU bounded by new `storage.mailbox_cache_size` (default 10,000, a memory bound not a rate); `MailboxServer.UpdateMailbox` writes storage then evicts, and the MMA handler goes through it; `MailboxServer.CacheStats()` (size, capacity) is reported in the `/readyz` details as `mailboxCache`; `client.UpdateMailboxConfig` added (the Go client had no updateConfig call). Tests: `test/integration/mailbox_cache_test.go` `TestMailboxCacheStaysWithinItsBound` (8 folders, bound 3: old code held 8), `TestUpdatedRetentionReachesTheNextDelivery` (retention 30→1 day; old code still clamped to the stale window). Audit B M5. |

## Architecture

| ID | Status | Item |
|----|--------|------|
| A1 | done 2026-09-13 | **Authorization is re-implemented in eleven places with four response shapes.** Each handler checked owner/ACL itself and answered differently: MAA expunge a bare `{"error": ...}` map, MMA `verifyOwner` an `AdminResponse` with a message and no status, MSA batch a `StoreAck` with a message and no status, SDA/SFA/SCA a 403 with an unprefixed header. Fix: `internal/protocol/wire/authz.go` `Forbidden(reason)`, `RequireOwner(sc, owner, action)` and `RequireSelf(sc, claimed, field)` all return `mailboxes.UnauthorizedError`, so `wire.Classify` gives 403 and an `unauthorized: ...` message in whichever envelope the protocol uses; every one of the seven handler check sites goes through them and sets `sc.Err` for its error writer (MSA batch classifies per ack). The mailbox-type ACL checks already returned `UnauthorizedError`. Tests: `internal/protocol/wire/authz_test.go` `TestAuthorizationRefusalsClassifyAsForbidden`; `test/integration/authorization_test.go` `TestUnauthorizedRequestsShareOneShape` (seven sites across all six protocols; on the old handlers three had status 0 and three lacked the prefix). Audit C M4; M3 (handlers bypassing the MDA) is not covered here. |
| A2 | done 2026-09-13 | **No unit tests for the handlers, mailbox types, registry or storage interface.** `internal/protocol/*/handler_test.go` did not exist; authorization was covered only by the DB-backed integration suite. Fix: `internal/storage/storagetest.Fake`, an in-memory `storage.Storage` (mailboxes, messages, ACLs, cursors, documents with history, feeds, collections with equality/comparison/$and/$or filters, directory; the SQL-only parts return `ErrNotFaked`), which also replaced the hand-rolled mock in `internal/mta/router_test.go`; `internal/protocol/protocoltest` (`New`, `Exchange` over `forgetest.NewMockStreamWithFrame`, `Decode`, `DecodeRetrieve` for the MAA compound frame, `Deliver`, `Mailbox`) drives a real pipeline with the MDA and MTA wired as server.go wires them. Tests: `internal/mda/mailboxes/mailbox_test.go` (5: private owner-only, shared ACL + cursor, public read-anyone/write-by-grant, rolling window prune-then-retry, paging/byte budget); `internal/protocol/{msa,maa,mma,sda,sfa,sca}/handler_test.go` (29 tests across the six protocols: every operation's success path, refusals by status, validation). Two defects the tests surfaced are fixed in the same commit: MMA `queryCapacity` before the first sample answered with a message and no status (now the classified 503); a bad collection filter operator reached the client as a 500 (new `storage.ErrInvalidFilter`, classified 400 with the operator named). Audit C M8. |
| A3 | done 2026-09-13 | **Client edge cases.** `DeleteDocument`, `DeleteCollection`, `DeleteCollectionItem` and `DeleteFeed` returned `(true, nil)` on any status but 404, so a 403 or 500 read as deleted; `PatchDocument` returned a result on failure without a typed error; `HeadDocument`, `ListCollectionKeys` and `QueryCollection` reported not-found as untyped strings; there was no `Close()`; server selection never went past the first preferred server. Fix: `pkg/client` `deleteOutcome` (404 → false/nil, 2xx → true, else the typed `ProtocolError` for the status) on all four deletes; patch/head/keys/query go through `responseError`; `Client.Close()` removes the notification handler and makes later calls fail with `ErrClosed` (the host is the caller's and stays open); `Client.dial` replaces the eleven `selectServer`+`openStream` pairs and tries preferred servers by priority then weight, passing over one that cannot be dialled, with the error naming every attempt; an explicit `With*Server` option makes one attempt. `GetDocument` keeps its status-in-result shape (a conditional get needs the 304). README client section documents the contract. Tests: `pkg/client/client_test.go` on loopback TCP hosts with canned servers — `TestDeleteOutcomeIsHonest`, `TestFailuresOnTheStoreProtocolsAreTypedErrors`, `TestDialFailsOverToTheNextPreferredServer`, `TestServersAreTriedByPriorityThenWeight`, `TestCloseRefusesFurtherRequestsAndUnregistersHandlers`. Audit C M9. |
| A4 | done 2026-09-13 | **Process lifecycle.** `cmd/ricochet/main.go` waited for one signal and a second was ignored while `Stop` hung; `internal/server/server.go` `isRunning` was a plain bool written by `Start`/`Stop` and read by the maintenance goroutine; a failure in `initializeP2P` after `initializeStorage` leaked the pool, and a failure in `startOpsAPI` leaked the pool, the host and the background goroutines. Fix: `waitForShutdown` (`cmd/ricochet/shutdown.go`) keeps listening while `Stop` runs and a second signal exits with 128+signal (130 for SIGINT, 143 for SIGTERM); `running` is an `atomic.Bool` and `Stop` is guarded by `CompareAndSwap`, so it is idempotent and safe from any goroutine; `Start` unwinds through the new `teardown()` (shared with `Stop`, same order: cancel → services → forge → limiters → ops → MDA, or storage directly when the MDA does not exist yet) on any failure, and a forge server whose own `Start` failed is dropped since it has already closed what it built. Tests: `cmd/ricochet` `TestSecondSignalForcesExitWhileStopHangs`, `TestGracefulStopReportsItsOutcome`; `internal/server` `TestStartFailureReleasesWhatItBuilt` (P2P bind failure and ops-port-in-use; asserts the pool is closed and a fresh server binds the same UDX port), `TestStopDoesNotRaceTheMaintenanceLoop` (run with `-race`). On the old code the pool stayed open in both cases and the race detector flagged the flag. Audit C M10. |
| A5 | done 2026-09-13 | **Dead schema and tags.** `schema.sql` `block_store` (+3 indexes) and the views `mailbox_stats`, `message_priority_stats` were referenced by no Go code; `ServerConfig` and the eight config types it nests carried 83 `yaml:` tags that nothing decodes (the file is parsed only through `yamlFileConfig`, with `KnownFields(true)`). Fix: `block_store` **stays**, documented in `schema.sql` as reserved for the roadmap's content-addressed body offload (`doc/SCALE_ROADMAP.md`, `doc/SCALABILITY.md` §7.7 plan to ship that against the table every deployment already has; shape provisional) and README's table says so; the two views are dropped (`DROP VIEW IF EXISTS` in the in-place upgrade section, CREATEs removed): no reader, and `mailbox_stats` recomputed `message_count` with a join over every stored message when the trigger-maintained column exists; the operator surface is the ops API. All 83 yaml tags removed from the runtime structs (json tags kept). Tests: `internal/core` `TestRuntimeConfigIsNotAYAMLTarget` (reflection: no yaml tag anywhere under `ServerConfig`), `internal/storage/postgres` `TestSchemaCarriesNoDeadViews` (no views in `public`, `block_store` present). Verified the upgrade path by hand: old schema then new schema on one DB, views 2 → 0. Audit C L3, M7. |

## Documentation

| ID | Status | Item |
|----|--------|------|
| D1 | done 2026-09-13 | **Quick-start database setup fails on a fresh server.** README "Database Setup" ran `createdb` and the schema, but `schema.sql` grants to a `ricochet` role the README never created; on a fresh cluster the first grant failed with "role ricochet does not exist" and the rest of the file (the in-place upgrades included) never ran. Fix: the PERMISSIONS section creates the role with `LOGIN` when `pg_roles` has no such row (a `DO` block, idempotent; an existing role is left untouched, so the supervisord guide's `CREATE USER … PASSWORD` flow is unchanged) and documents that a role created this way has no password; README creates the role with the same password its run example exports. Stale note about view privileges removed. Test: `internal/storage/postgres` `TestSchemaLeavesTheServiceRoleAbleToWork` (role can log in; `has_table_privilege`/`has_sequence_privilege` on every public table and sequence). Fresh-cluster path verified by hand with the role name substituted: new schema creates it and grants everything; old schema fails at the first grant. |
| D2 | done 2026-09-13 | **README facts.** Every README-located audit row (Appendix D) re-verified against current code; most had been fixed by the S/P/A rows. Remaining fixes in README: "production-ready" dropped; placeholder repository link replaced by the module path; relay bullet says AutoRelay/hole punching are off by default and take candidates from `bootstrap_peers`; "health monitoring" replaced by the real two-hour staleness cutoff; protocol table gains MSA batch, SFA, SCA and notify; Go 1.25+; CLI block gains `--high-capacity`, `--config`, `--external-addrs`, `--debug-dht` and honest preset descriptions; "never sees plaintext" qualified to the payload; identity file is `peer_identity.key`; UDX described as reliable; NAT snippet shown in YAML (`features.enable_*`, `server.bootstrap_peers`); uptime score stated as a constant 1.0; project structure regenerated from the tree; bench section lists every flag and scenario, says MMA is not benchmarked, carries a real run, and says resources are created before warmup; test section drops stale counts; schema table lists every table. Two gaps found on the way were fixed in code rather than reworded: **`--port` and `server.port` never took effect** (the default listen address hard-codes 55223 and forge derives an address from `Port` only when the list is empty) → `ServerConfig.SetPort` rewrites the port in every listen address, used by the flag and the file loader; and **`mixed` bench never hit SCA** although it created collections → draws from all five. Tests: `internal/core` `TestReadmeMatchesTheCode` (Go version from go.mod, every registered protocol ID, every server and bench flag, every bench scenario, every schema table, every package directory, identity file name, UDX wording; fails the old README at 19 points), `TestSetPortRewritesTheListenAddresses`, `TestFilePortReachesTheListenAddresses`. Audit Appendix D. |
| D3 | done 2026-09-13 | **Supervisord guide.** Walked `doc/SUPERVISORD_DEPLOYMENT.md` against `deploy/`, `docker-build.sh` and the package contents. Fixed in the guide: `RICOCHET_MAX_MEMORY` (nothing reads it) replaced by the real bounds (`performance.max_concurrent_connections`, `admission_control`, `database.pool_size`, `storage.mailbox_cache_size`, `/ops/limits`) and the honest statement that supervisor cannot cap memory; firewall line says UDP 55223 in and ops 9090 loopback-only, not 4001; links to `doc/LINUX_POSTGRES_DEPLOYMENT.md` and `deploy/README.md` (neither exists) replaced by README, `config.example.yaml`, the ACL doc and SCALABILITY; `alert.sh` (never defined) replaced by a `logger` line; build section says the package build needs Docker; the `DB_SSLMODE` consequence of `--production` is stated at the configure step; `stopwaitsecs=30` explained against the server's real shutdown sequence; supervisor excerpt labelled as the shipped file. Gaps fixed in the deploy files rather than the prose: the guide's inline health check is now shipped as `deploy/health_check.sh`, installed by the package at `/opt/ricochet/health_check.sh` (`docker-build.sh`) and copied in the manual install; **an upgrade left the server stopped** ("Service will auto-restart" was false: `prerm` stopped it and `supervisorctl update` only restarts programs whose config changed) → `prerm` records a running service on `upgrade` and `postinst` starts it again, first installs stay stopped until the password is set; the packaged `config.yaml` carried a live `/ip4/YOUR_PUBLIC_IP/...` external address (skipped with a warning at every start) → commented out with a real example, `EXTERNAL_IP` in the env file is the documented path (this is the first half of D4; its pool-size half remains). Test: `internal/core` `TestDeploymentGuideMatchesTheDeployFiles` (every `deploy/` and `doc/` path exists; every `/opt|/etc|/var/*/ricochet/...` path is produced by `docker-build.sh`, `postinst` or the supervisor config; every `RICOCHET_*` variable is read by the code; ports are `DefaultConfig().Port` and `DefaultOpsConfig().Port`, never 4001; the supervisor excerpt matches the shipped file line for line; fails the old guide at six points). Audit claims #88–#92. |
| D4 | done 2026-09-13 | **Debian package installs the example config verbatim.** `docker-build.sh` copied `config.example.yaml` to `/etc/ricochet/config.yaml`, which the server loads over the production preset `run.sh` selects; the example's live `pool_size: 10` replaced production's 50 (and admission control, sized from the pool, followed it down), `region: us-east-1` was advertised as fact, and the placeholder external address was live (fixed in D3). Measured: loading the old example over `ProductionConfig()` changed `PoolSize` 50→10, `ServerRegion`, `DataDirectory`. Fix: new `deploy/config.yaml`, comments only, installed as `/etc/ricochet/config.yaml` (`docker-build.sh`; the example stays beside it as `config.yaml.example`; the guide's manual install copies both); `config.example.yaml` now keeps its own promise that every live value is the default except `data_directory` and `max_storage_gb`: `pool_size` 25 (production 50 noted), `region` `default`. Tests: `internal/core` `TestPackagedConfigChangesNothing` (`deploy/config.yaml` loaded over the production and default presets leaves every field unchanged), `TestExampleConfigDocumentsTheDefaults` (the example over `DefaultConfig()` differs only in the two declared fields); against the old files the first reports the three production changes and the second the two non-default values. `TestExampleValuesActuallyApply` updated to the new pool size (it starts from a pool of 2, so it still proves the key applies). |
| D5 | done 2026-09-13 | **Bench docs.** Of the row's four items, D2 had already fixed `mixed` (now all per-operation scenarios) and the README bench section. Done here: **`mma` scenario** added to `cmd/ricochet-bench` (`CreateMailbox` + `DeleteMailbox` per request, in the mix too; flag help, usage text and README row updated); `doc/BASELINES.md` gains a same-session table of every per-operation scenario including `mma` and the six-way `mixed`, the old `mixed` row is footnoted as measured without `sca`/`mma`, the reproducing notes say not to bench on `--development` (debug logging) or a loaded host; the "NUL escape returns 500" known issue was **fixed rather than kept**: `storage.ErrInvalidContent` wraps SQLSTATE 22P05/22021 from collection inserts and updates and `wire.Classify` makes it a 400 with the database's reason. Tests: `cmd/ricochet-bench` `TestMixedCoversEveryPerOperationScenario` (the mix draws from every non-batch, non-sync scenario in `validProtocols`), `TestBaselinesDocumentEveryScenario` (every scenario has a row in BASELINES.md); `internal/storage/postgres` `TestCollectionContentTheDatabaseCannotHoldIsInvalidContent`; `internal/protocol/wire` `TestInvalidContentClassifiesAsBadRequest`. Finding raised as N8: this session's numbers are 5–10× below the recorded baseline on a saturated host. |

## Found since the audit

| ID | Status | Item |
|----|--------|------|
| N1 | done 2026-09-14 | **`health_check_interval` is still unread.** The key exists (`internal/core/config.go`, `config.example.yaml`) and nothing consumes it. Noted during B7. `presence_check_interval` was in the same state until P4 made it the presence reconcile interval (2026-09-13). Fix: wire them or remove them from the config and the example (the example test will catch an orphaned key). **Wired, not removed.** `health_check_interval` is now the registry's self-check and heartbeat period: every interval the registry runs the server's readiness checks (`Server.selfCheck`, the same `readinessChecks()` behind `/readyz`, 10s timeout) and publishes a signed `Heartbeat` (`server_id, healthy, uptime_score, timestamp`) on `HeartbeatTopic`, which until now was joined and never used. The announced `uptime_score` is measured: the share of the last 288 checks that passed (24h at the default 5m), no longer a constant 1.0; the first check runs in `Start` so the first announcement carries a real figure. `SFServerInfo` gains `healthy`; registries subscribe to the heartbeat topic, refresh an announced entry's timestamp, score and health from each heartbeat (same signer rule as announcements; an unannounced server's heartbeat is dropped; entries are replaced, not edited, since `GetAvailableServers` hands out pointers), and `GetAvailableServers`/`GetStats` exclude a server whose last verdict was unhealthy. `GetStats` also reports own `healthy`, `uptime_score`, `self_checks`. README discovery text and the example's `health_check_min` comment say so. Tests: `internal/registry` `TestUptimeScoreTracksFailedChecks` (score = passed/total, latest verdict, window forgets after 288), `TestHeartbeatMustBeSignedByItsServer` (forged and unannounced heartbeats change nothing; unhealthy removes, healthy restores with the heartbeat's score and timestamp), `TestHeartbeatsKeepAHealthyServerFresh` (real GossipSub, 100ms interval, announcements 1h apart: heartbeats refresh the entry, a failing self-check drops the server within an interval, a recovering one returns with the failures still in its score); `internal/core` `TestExampleValuesActuallyApply` now asserts `intervals.health_check_min` reaches the config. |
| N2 | done 2026-09-14 | **go-libp2p 0.47.0 with quic-go 0.60.0 is not the upstream pairing.** B11 bumped quic-go for an advisory; go-libp2p 0.49.0 is the release built against it and needs Go 1.25.7+. Bump go-libp2p, forge and the transport together, in lockstep with the Dart clients (`udx-wire-v3-lockstep`). **Done:** go-libp2p 0.49.0 in all three modules; forge tagged `v0.1.3` (`70b2178`), transport `v0.1.1` (`c79c005`), both pushed; go-udx untouched (no libp2p dependency, stays `v0.1.0`); kad-dht stays pinned at 0.37.1 (N3), which compiles against 0.49.0. `go` directive is now 1.25.7 everywhere; `Dockerfile.build` installs go1.26.8 with a sha256 check (was go1.25.0, which could no longer build the module); README states the floor. No Dart lockstep needed: the 0.48/0.49 release notes carry no yamux, noise, multistream, relay or DCUtR wire change (identify bounds the protocol count a peer may advertise; unspecified listens advertise every interface address), and the transport's Dart interop tests pass unchanged. Suites: ricochet full run green under 0.49.0; forge and transport suites green. Tests: `internal/core` `TestDockerBuildUsesAToolchainTheModuleAccepts` (Docker's Go release must not fall below go.mod's directive; fails the old Dockerfile), `TestReadmeMatchesTheCode` now checks the full go directive, patch level included. |
| N3 | done 2026-09-14 | **kad-dht is pinned to v0.37.1 by a replace.** Forge requires 0.37.1 directly; something in the graph asks for 0.38.0 and the replace holds it back. Find the requester (`go mod graph | grep kad-dht@v0.38.0`) and either move everything to 0.38.x or record why not. **Done:** the 0.38.0 requester was ricochet's own stale `// indirect` line, nothing else in the graph; the pin dates from the forge migration (`b8a7a97`) with no reason recorded. Moved everything to the current release instead: forge `v0.1.4` (`ee179a2`, pushed) builds on kad-dht 0.42.2, whose only change reaching us is the constructor losing its context argument (0.42.0; nothing of ours stores DHT value or provider records, so the namespaced record keys are moot). Ricochet drops the replace and takes forge v0.1.4; `go mod graph` now agrees on 0.42.2 (boxo asks 0.40.0, forge and ricochet 0.42.2). Full suite green on a fresh database; forge suite green. While here: the relay-only routing table filter that the DHT debugging notes describe as added to forge (`dht.RoutingTableFilter(directlyReachableFilter(...))`) does not exist in any commit of forge or ricochet → N9. |
| N4 | done 2026-09-14 | **Dart client trusts presence and registry payloads.** `../ricochet-dart-client/lib/client/presence_tracker.dart` applies any message on a server's topic and `lib/registry/peer_preferences.dart` reads `server_id` from the payload, the same flaw S2 closed server-side. Fix: compare the pubsub message's signed `from` with the topic's server / the payload's id before applying. Found during S2 (2026-09-13). **Done (presence half), ricochet-dart-client `PresenceTracker.handleMessage(from, server, data)`:** a message on a server's topic is applied only when `message.from` (the publisher whose signature dart_libp2p_pubsub verifies before delivery; `receivedFrom` is just the relaying neighbour) is that server and the payload's `serverId` names it; otherwise counted in `rejectedMessages` and dropped. Same rule as the Go tracker. **Registry half not applicable:** `peer_preferences.dart` is a local data structure (`PreferencesManager` is in-memory, "future enhancement"); no Dart code subscribes to `/sf-network/services/announce` or `/heartbeat`, so there is no payload to verify. `pubspec.yaml` gains the `dart_udx` path override overnode_v2 uses, without which `dart test` could not resolve. Test: `test/presence_tracker_test.dart` (forged event/heartbeat on the server's topic changes nothing; payload naming another server refused; genuine ones apply; fixtures round-trip the Go wire shape). Dart repo committed, not pushed (path dependency; no push needed). |
| N5 | done 2026-09-14 | **Dart client has no payload encryption, so no parity with the Go client's bound NaCl box (S8).** `../ricochet-dart-client/lib/client/message_sender.dart` has no encrypt option; it only reads the `encrypted` flag bit. Go and Dart clients cannot exchange encrypted messages, and Dart apps without their own ratchet get no mailbox confidentiality. OverNode is Signal-encrypted above this layer (forward secrecy there), but Signal does not detect a server re-filing a message into another folder or id; the binding would. Fix, in the Dart client: Ed25519→X25519 conversion matching `pkg/client/encryption.go` (pinenacl has the libsodium maps); the bound format only (`"RCE2"` ‖ 24-byte nonce ‖ box(header ‖ payload), header = three u16-length-prefixed fields: recipient peer ID, folder path with empty = `inbox`, message ID); reading the legacy unbound format only if old Go-client messages must be readable; an opt-in `encrypt` flag, off by default; cross-language test vectors (fixed keys and nonce) checked into both repos. Blocked on that repo's `dart pub get` conflict (dart_udx path vs hosted, see N4 notes). OverNode turning the flag on is a separate, later step: double envelope per message (~150 B overhead, negligible CPU), every fielded build must decrypt before any build encrypts, and the client must say which layer failed on a decrypt error. Raised 2026-09-13 after S8. **Done in ricochet-dart-client (`lib/crypto/payload_encryption.dart`, committed, not pushed):** `PayloadEncryptor` (from the Ed25519 seed; dart-libp2p keys built from a seed do not retain it, so the seed is supplied explicitly, `fromLibp2pKey` works only for keys that still hold it), `PayloadBinding`, `encryptBound`/`decryptBound` in the Go format, legacy format still opens (reported unbound), `openIfEncrypted` for retrieval. pinenacl 0.6.0 supplies NaCl box and the libsodium Ed25519→X25519 maps. `SFClient(encryptor:)` + `sendMessage(encrypt: true)` (off by default; mints the message id before sealing) and `retrieveMessages` opens against the arriving envelope; a decrypt failure is a `PayloadDecryptException` naming this layer. Cross-language vectors (seeds 0x01/0x02, nonce 0..23, "hello, ricochet") checked into both repos: Go `pkg/client` `TestCrossLanguageVectors`, Dart `test/payload_encryption_test.dart` (peer ids, X25519 keys, bound and legacy ciphertext byte for byte, relabelling refused, retrieve-side re-filing refused). READMEs on both sides describe the layer. OverNode turning the flag on remains a separate step, as the row says. |
| N6 | done 2026-09-14 | **Documents, feeds and collections have no read authorization.** S7 documented that any peer can read every document, feed and collection; the handlers check ownership on writes only (`internal/protocol/sda/handler.go:281-286`, `sca/handler.go:171`, SFA the same) and the `documents`, `feeds` and `collections` tables in `schema.sql` have no visibility column. Mailboxes already have the model wanted here: private, shared with an ACL (`mailbox_acls`), public. No client uses these stores yet (OverNode speaks only submit, access, admin and presence), so the defaults can still change without breaking anyone; that window closes once a client ships against them. Fix: a visibility column plus ACL table for documents, feeds and collections, set at create/put and enforced on every read (`GET`, `HEAD`, `LIST`, `HISTORY`, `BATCH_GET`, `QUERY`), with visibility a property of the whole version stream so a document made public later does not expose private history; defaults owner-only for documents and collections, public for feeds; directory stays public by nature. Peer identity is already Noise-authenticated on every stream, so this needs no new key material and keeps PATCH, JSONB `QUERY`, If-Match and directory search working. Deliberately not server-side encryption (protects a stolen disk only; recommend volume encryption or PostgreSQL TDE in a deployment note instead) and not client-side content encryption (kills server-side reads and needs group key management; leave to applications, possibly a library helper after N5). Do after the P rows since it adds a predicate to the read hot path. Update the S7 table in the README when done. Raised 2026-09-13. **Done:** the three stores carry the mailbox model. `visibility SMALLINT` on `documents`, `feeds`, `collections` (private, shared, public; defaults private, public, private; in-place upgrade gives existing rows the same defaults) plus reader-list tables `document_acls`, `feed_acls`, `collection_acls` (cascade with their resource). Set at creation (`visibility` on document `PUT`/`BATCH_PUT`, feed and collection `CREATE`) and managed with a new owner-only `ACCESS` operation on each protocol (`accessAction` get/set/grant/revoke, `readerPeerId`), answering with the visibility and reader list. One predicate, `wire.RequireReader`, runs on every read after the record is fetched: document `GET`/`HEAD`/`HISTORY` (both forms), feed `GET` (metadata, entry, page), collection `GET`/`LIST` keys/`QUERY`; `LIST` of documents, feeds and collections filters in SQL by reader (`readableBy` predicate, so paging cursors stay consistent); `BATCH_GET` checks each feed and reports a refused one as that feed's error. Visibility covers the whole resource (history, entries, items). A refused read is a 403 like a refused write. Go client: `WithVisibility`/`WithFeedVisibility`/`WithCollectionVisibility`, `Visibility` in every info type, and `{Document,Feed,Collection}Access`/`Set…Visibility`/`Grant…Reader`/`Revoke…Reader`. README table rewritten as "Who Can Read What", schema table lists the new tables, doc Gotcha 6 rewritten. Tests: `internal/protocol/{sda,sfa,sca}` `TestDocumentVisibility`, `TestFeedVisibility`, `TestCollectionVisibility` (fake store; every read op per visibility, listing filter, batch per-feed refusal, ACCESS owner-only, grant inert until shared, revoke, put keeps/changes visibility); `internal/storage/postgres` `TestStoreVisibilityAndReaders` (column round trip, set by path, grant idempotent, list/revoke, cascade, per-reader listings); `test/integration` `TestStoreVisibilityEndToEnd` (two clients over the wire). Not done, as the row says: server-side encryption; Dart client fields (additive JSON, Dart frames ignore unknown keys). |
| N7 | done 2026-09-14 | **`TestSearchDirectory` is not re-runnable on a reused database.** `test/integration/directory_test.go:99` expects exactly one listing matching `encryption`, and every run joins another one, so the second run on the same database fails (seen 2026-09-13 during P7; passes on a fresh database, which is how the suite is normally run). Fix: give the test's listings a per-run word, or count only its own peers. **Done:** the bio carries a word minted per run (`zq` + nanosecond hex), the search is for that word, and the one hit must be the test's own peer. Run three times on one database: green each time; the old test failed on its second run with two matches. |
| N8 | done 2026-09-14 | **Per-operation throughput unexplained.** The 2026-09-13 bench session (`doc/BASELINES.md`, session table) came out 5–10× below the `ca32f10` figures with p99 stalls of 100–400ms, taken on a host at load average 22 with 0% idle and five Docker containers up. Not diagnosable in that state. Fix: on an idle machine, re-take the per-operation rows at HEAD, then A/B against `ca32f10` in one session (its worktree must sit beside the sibling repos: that commit uses relative `replace` directives to `../go-p2p-forge` and `../go-libp2p-udx-transport`, and their current checkouts may not build it — check out the siblings at the commits `go.mod` names). Suspects if it reproduces: per-request deadlines, admission acquire, the access log (about two lines per request at INFO), the capacity write gate. Found during D5. **Done:** it was the host. HEAD (`8ce3df7`) and `ca32f10` ran side by side on an idle machine (both servers up at once, fresh databases, one bench binary, two passes in opposite order, then `msa`/`maa` at 5,000 requests): within about 10% on `msa`, `sda`, `sfa`, `sca`, `mma`, `mixed`, no stalls on either. The one gap, `maa` at half the rate, is the non-destructive retrieve returning ten real messages where `ca32f10` had drained the seeds and was answering empty (its database ended 5,015 messages lighter). Every suspect cleared. Table and reading in `doc/BASELINES.md`, "Session of 2026-09-14". Note for next time: `ca32f10`'s `-port` flag does not move the listen address (the default listen multiaddr keeps 55223), so drive it by config file; six orphaned `while :; do :; done` shells from another session's finished script were on the host again and had to be killed first. |
| N9 | done 2026-09-14 | **The relay-only routing table filter was never committed.** The DHT stability notes (memory `debugging-udx-relay`, "Fix 1") say forge's `node/node.go` sets `dht.RoutingTableFilter(directlyReachableFilter(h, logger))` so peers reachable only over a circuit relay are kept out of the server's DHT routing table: with them in, CPL refresh queries to relay-only mobile clients fail, yamux GOAWAYs tear the connections down, and the routing table drains to zero. `git log --all -S directlyReachable` finds nothing in forge or ricochet (2026-09-14, found during N3). **Done, with a corrected premise.** Probed on go-libp2p 0.49.0 / kad-dht 0.42.2 with a real relay and reservation: a relay-only peer never enters the routing table even with no filter, because kad-dht's liveliness probe cannot open a stream over a limited connection (it never opts in), so the filter as described would have been a no-op. What does get admitted is the actual mobile case: a peer that dials the server over a direct connection and advertises the DHT protocol is added on the strength of that inbound connection, and the server cannot dial it back. forge `v0.1.5` (`node/filter.go`, pushed) sets `dht.RoutingTableFilter(dialableFilter(h, logger))`: a peer is admitted only while the server holds a direct connection to it that the server opened; relayed connections (limited, or `/p2p-circuit` in the remote address) are refused whichever side opened them. Cost stated in the code: a peer that only ever dialled us stays out until we dial it (kad-dht dials peers it learns from lookups; bootstrap peers are dialled at start). Ricochet takes forge v0.1.5. Test: forge `node` `TestOnlyPeersWeHaveDialledEnterTheRoutingTable` (relay-only peer, a peer we dial, a peer that dials us; fails without the filter because the inbound-only peer is admitted). Ricochet full suite green. Memory notes corrected. |
| N10 | open | **UDX acknowledges every data packet at once, from the one read loop that also receives.** Profiling the `sca` row (`doc/BASELINES.md`, "where the `sca` time goes", 2026-09-14) put the whole gap in the transport: a 137 KB query page is about 100 datagrams of 1,372 bytes (`go-udx` `stream.go` chunks at `MaxDatagramSize - 100`) and the receiver answers each with an ACK datagram immediately (`connection.go` `HandlePacket` → `sendPacket(buildAckFrame(seq))`, `AckDelay` always 0; `MaxAckDelay` exists as a constant and only caps the RTT sample). The multiplexer's single `readLoop` per socket handles every datagram inline, ACK send included, so during the query run the server's loop spent 7.4 s in `recvfrom` and 6.9 s in `sendto` of a 15.1 s profile: one core, saturated, while the rest of the server idled; the client burned 3.5 cores, 53% of it under the ACK it sends per packet received. `buildAckFrame` also walks the whole received-sequence map (`recvdDataSeqs`, up to 500 entries) on every ACK: 5.5% of client CPU. A put moves 25 datagrams per request, the default query page 246. Fix, in go-udx (our repo; wire-compatible for a sender that reads ACK ranges, which ours does, but the Dart UDX must be checked for RTT sampling and loss detection on fewer ACKs before shipping, `udx-wire-v3-lockstep`): (1) ACK every second ack-eliciting packet or after a short delay (QUIC's rule, `MaxAckDelay` is already declared), and always at once on a gap or a FIN; (2) keep the largest sequence and the contiguous range incrementally instead of iterating the map; (3) take the ACK send off the read loop, or batch sends (`sendmmsg` on Linux, where production runs; macOS has no equivalent, so measure there in Docker). Expect the `sca` row to roughly double from (1) alone; re-take every per-operation row after, since the same loop carries them all. Recipe to re-measure: a scratch module beside the repo with `replace github.com/twostack/go-ricochet => <repo>`, a `main` that builds a client like `cmd/ricochet-bench` `createWorker` and runs one operation per request: `-mode put` (new keys), `put-update`, `query` (default page), `query1` (`WithQueryLimit(1)`), `list`, `bench` (put then query), with `-prefill <items per collection>`, `-payload-size`, `-limit`, `-cpuprofile`; server started with `ops.enable_pprof: true` and `go tool pprof -top http://127.0.0.1:<ops>/debug/pprof/profile?seconds=15` during the run; `netstat -s -p udp` before and after for datagram counts. Numbers to beat are in the baselines section. |
| N11 | open | **Every first page of a collection query counts the whole collection.** `internal/storage/postgres/collections.go:284` and `:366` select `COUNT(*) OVER()` alongside the page; the window aggregate has to consume every row matching the filter before `LIMIT` applies, so `EXPLAIN` shows the index scan returning all 200 rows of a 200-item collection for a 51-row page, and the cost grows linearly: at 5,000 items the default page went from 5.8 ms to 7.6 ms p50 and a 1-item page from 1.4 ms to 1.9 ms (2 workers). Found while profiling `sca` (2026-09-14). Fix: for an unfiltered first page use `collections.record_count`, which every put and delete already maintains (`:158`, `:222`); for a filtered query either count in a separate statement only when the caller asks for a total (a `wantTotal` in the request, default off for the Go client's paging helpers which follow cursors anyway) or cap the count (`SELECT COUNT(*) FROM (... LIMIT 10001)`) and report "more than". Keep the `-1` on cursor pages as now. Test: a storage test that seeds a few thousand items and asserts the plan (or `pg_stat_statements` rows read) for the first page does not scale with the collection, plus the existing paging tests. |
| N12 | open | **A collection query page is JSON inside base64 inside JSON.** `internal/protocol/sca/handler.go` `handleQuery` marshals the items and base64-encodes the result into `Body`, so a 103 KB page is 137 KB on the wire and the client (`pkg/client/collections.go` `QueryCollection`) base64-decodes and re-parses it; `json.Marshal` of the page is 6% of the server's CPU in the query profile, most of it `json.RawMessage` re-validating each item's content. Documents carry bytes, so base64 is right there; collection items are JSON by definition. Found while profiling `sca` (2026-09-14). Fix: an `items` field on the response (raw JSON array, `json.RawMessage`) with `Body` left empty, the Go client reading `items` when present and falling back to `Body`; the same for `GET` of a single item if it takes the same route. This is a wire change on a protocol no shipped client speaks yet (N6 said the same of the visibility fields), so do it before a Dart collection surface exists. Third fewer bytes per page, and with N10 the datagram count follows. Test: handler test asserting the page is not base64, client round trip, and the bench `sca` row re-taken. |
